{
  "name": "QuantumVault",
  "version": "1.4.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "selftest": "node scripts/selftest.mjs",
    "check": "npm run selftest && npm run build",
    "deploy": "npm run build"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "@noble/post-quantum": "^0.5.4",
    "shamir-secret-sharing": "^0.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
